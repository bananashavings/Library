//TODO:
//Clicking table selects checkbox
//Searchbar
//Template for table?

let library: Book[] = [];

class Book {
    name: string;

    constructor(name: string) {
        this.name = name;
    }
}

function addToLibrary(book: Book) {

}

function render(books: Book[]) {
    books.forEach(book => {
        
    });
}